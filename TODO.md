# TODO: Make Restaurant Project Good and Running

## Tasks
- [x] Create server/models/models.py with User, Reservation, and MenuItem models
- [x] Create server/app.py with Flask app setup, database configuration, JWT, CORS, and blueprint registration
- [x] Create .env file with DATABASE_URL for MySQL connection
- [x] Install Python dependencies from requirements.txt
- [x] Create the MySQL database 'restaurant_db'
- [x] Run init_db.py to initialize the database with admin user and sample menu items
- [x] Run the Flask server (server/app.py)
- [x] Serve the client-side files using a simple HTTP server in client/ directory
- [x] Test the application by accessing the client and server endpoints
